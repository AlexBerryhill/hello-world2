// Header File (Similar to libraries) for i/o functions like cout
#include <iostream>
using namespace std;

//main is where all programs begin in C++
int main() {
  // cout will print the data in the quotes to the command line
  cout << "Hello World!\n";// "\n" means next line, like hitting enter
  // Always return something in C++
  return 0;//The Zero doesn't matter in this case, any integer works.
}